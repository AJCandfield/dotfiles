# Editor
alias vi="vim"
alias vim="nvim"

# Eza
alias ls="eza --color=auto --icons=auto --group-directories-first"
alias ll="ls -la"
alias la="ls -la"
alias l="ls"

# Git
# Move to the repository root folder. Errors if not in a git project.
alias gr='cd $(git rev-parse --show-toplevel)'

# Lazygit
alias lg="lazygit"
